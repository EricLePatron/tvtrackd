// Nom de l'app — piste "tvtrackd" à l'étude, vérification de risque de marque en cours (voir CLAUDE.md).
// Toujours en minuscules dans le code. Copie locale de src/lib/config.ts : les edge functions Deno
// ne partagent pas le bundler de l'app, donc pas d'import cross-répertoire vers src/. Garder les
// deux fichiers synchronisés.
export const APP_NAME = "tvtrackd";
