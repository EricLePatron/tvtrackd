Déployer les deux edge functions `trending-media` et `new-releases-media` via `supabase--deploy_edge_functions`, puis vérifier avec `supabase--curl_edge_functions` que chacune renvoie bien un tableau `results` non vide. Si erreur, consulter `supabase--edge_function_logs` pour diagnostiquer (probablement `TMDB_API_KEY`).